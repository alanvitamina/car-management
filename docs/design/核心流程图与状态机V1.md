# 公务用车管理系统：核心流程图与状态机 V1

## 1. 文档说明

本文档用于明确公务用车管理系统的核心业务流程、状态流转与关键节点处理规则，作为后续原型设计、接口设计、数据库设计和开发实现的依据。

相关文档：

- [系统架构与流程边界V1.md](系统架构与流程边界V1.md>)
- [需求分析文档V1.md](需求分析文档V1.md>)

## 2. 流程设计原则

- 业务主状态统一由新系统维护
- 飞书承载审批动作，但不承载业务主状态
- 审批撤回、移交、加签、会签记录在审批记录中
- 派车前必须二次校验资源可用性
- 核算以“已确认消耗”为最终依据
- 历史状态不可覆盖，只能流转

## 3. 核心流程图

### 3.1 公务用车主流程

```mermaid
flowchart TD
    A[员工在飞书进入系统] --> B[填写公务用车申请]
    B --> C[系统校验必填项与时间合法性]
    C --> D[自动带出部门负责人和行政经理]
    D --> E[生成申请单]
    E --> F[推送飞书一级审批]
    F --> G{一级审批结果}
    G -->|驳回| H[申请单变为审批驳回]
    G -->|通过| I[推送飞书二级审批]
    I --> J{二级审批结果}
    J -->|驳回| H
    J -->|通过| K[进入待派车]
    K --> L[行政经理分配车辆和司机]
    L --> M[系统二次校验车辆和司机冲突]
    M --> N{校验是否通过}
    N -->|否| K
    N -->|是| O[预占车辆和司机]
    O --> P[通知申请人和司机]
    P --> Q[司机执行任务]
    Q --> R[司机录入消耗]
    R --> S[通知用车人确认]
    S --> T{确认结果}
    T -->|驳回| U[退回司机补录]
    U --> R
    T -->|确认| V[核算司机补助]
    V --> W[恢复车辆和司机状态]
    W --> X[申请单完成]
```

### 3.2 私车公用主流程

```mermaid
flowchart TD
    A[员工在飞书进入系统] --> B[填写私车公用申请]
    B --> C[系统校验申请信息]
    C --> D[自动带出部门负责人和行政经理]
    D --> E[生成申请单]
    E --> F[推送飞书一级审批]
    F --> G{一级审批结果}
    G -->|驳回| H[申请单变为审批驳回]
    G -->|通过| I[推送飞书二级审批]
    I --> J{二级审批结果}
    J -->|驳回| H
    J -->|通过| K[进入待执行]
    K --> L[用车人录入起始里程 结束里程 起止地点和费用]
    L --> M[系统自动计算记录总里程]
    M --> N[行政经理核验导航里程]
    N --> O{偏差是否合理}
    O -->|是| P[采用记录总里程]
    O -->|否| Q[采用导航核验里程]
    P --> R[计算私车公用补助]
    Q --> R
    R --> S[汇总过路过桥费和停车费]
    S --> T[申请单完成]
```

### 3.3 申请取消流程

```mermaid
flowchart TD
    A[发起取消] --> B[系统校验当前状态是否允许取消]
    B --> C{是否在审批中}
    C -->|是| D[同步撤回或终止飞书审批实例]
    C -->|否| E[继续取消]
    D --> E
    E --> F{是否已预占车辆或司机}
    F -->|是| G[释放车辆和司机资源]
    F -->|否| H[直接写入取消状态]
    G --> H
    H --> I[记录取消原因与操作日志]
```

### 3.4 申请变更流程

```mermaid
flowchart TD
    A[发起变更] --> B[复制原申请生成变更单]
    B --> C[修改时间 地点 类型等字段]
    C --> D[重新校验资源冲突]
    D --> E[重新进入审批流程]
    E --> F[保留原单与变更单关联]
```

## 4. 关键状态机

### 4.1 申请单状态机

```mermaid
stateDiagram-v2
    [*] --> 草稿
    草稿 --> 待一级审批: 提交申请
    待一级审批 --> 审批驳回: 一级驳回
    待一级审批 --> 待二级审批: 一级通过
    待一级审批 --> 已取消: 申请人撤回/取消
    待二级审批 --> 审批驳回: 二级驳回
    待二级审批 --> 审批通过待派车: 二级通过且为公务用车
    待二级审批 --> 待执行: 二级通过且为私车公用
    待二级审批 --> 已取消: 申请人撤回/取消
    审批通过待派车 --> 已预占待执行: 派车成功
    审批通过待派车 --> 已取消: 取消且释放资源
    已预占待执行 --> 执行中: 实际出车
    已预占待执行 --> 已取消: 未出车前取消
    待执行 --> 待消耗确认: 提交消耗
    执行中 --> 待消耗确认: 提交消耗
    待消耗确认 --> 执行中: 确认驳回后补录
    待消耗确认 --> 已完成: 确认并核算完成
    审批驳回 --> [*]
    已取消 --> [*]
    已完成 --> [*]
```

说明：

- 私车公用一般不进入“审批通过待派车”和“已预占待执行”两个状态
- 审批移交、加签、会签不改变申请单主状态，仅影响审批完成条件和审批记录

### 4.2 审批记录状态机

```mermaid
stateDiagram-v2
    [*] --> 待处理
    待处理 --> 已通过: 审批通过
    待处理 --> 已驳回: 审批驳回
    待处理 --> 已撤回: 发起人撤回
    待处理 --> 已移交: 审批移交
    待处理 --> 加签中: 发起加签
    待处理 --> 会签中: 发起会签
    加签中 --> 已通过: 加签全部通过
    加签中 --> 已驳回: 任一加签驳回
    会签中 --> 已通过: 会签通过条件满足
    会签中 --> 已驳回: 会签驳回条件触发
```

### 4.3 车辆状态机

```mermaid
stateDiagram-v2
    [*] --> 空闲
    空闲 --> 已预占: 派车成功
    已预占 --> 出车中: 实际出车
    出车中 --> 空闲: 消耗确认完成
    空闲 --> 维修中: 送修
    维修中 --> 空闲: 维修完成
    空闲 --> 停用: 停用
    停用 --> 空闲: 重新启用
    已预占 --> 空闲: 取消或释放
```

### 4.4 司机状态机

```mermaid
stateDiagram-v2
    [*] --> 在岗可派
    在岗可派 --> 已预占: 派车成功
    已预占 --> 出车中: 实际出车
    出车中 --> 在岗可派: 消耗确认完成
    在岗可派 --> 休息: 排班休息
    在岗可派 --> 请假: 请假
    休息 --> 在岗可派: 恢复在岗
    请假 --> 在岗可派: 销假
    在岗可派 --> 停用: 停用
    停用 --> 在岗可派: 重新启用
    已预占 --> 在岗可派: 取消或释放
```

### 4.5 消耗记录状态机

```mermaid
stateDiagram-v2
    [*] --> 草稿
    草稿 --> 待确认: 司机提交
    待确认 --> 已驳回: 用车人驳回
    已驳回 --> 待确认: 司机补录后提交
    待确认 --> 已确认: 用车人确认
    已确认 --> [*]
```

说明：

- 公务用车的消耗记录由司机提交、用车人确认
- 私车公用的里程与费用记录由用车人提交，行政经理核验确认

## 5. 关键节点说明

### 5.1 为什么需要“已预占”状态

审批通过与实际出车之间可能存在时间差。若审批通过后不立即锁定资源，后续申请可能重复占用同一车辆或司机，因此需要在派车后进入“已预占”状态。

### 5.2 为什么需要“审批记录状态”与“申请主状态”分离

审批存在移交、加签、会签和撤回等复杂动作。如果把这些动作直接映射到申请单主状态，会导致主状态复杂失控。更合理的方式是：

- 申请单维护业务主状态
- 审批记录维护审批过程状态

### 5.3 私车公用为什么改为“总里程 + 导航核验里程”

根据当前已确认业务规则，私车公用由用车人录入出车前里程和结束后里程，系统自动计算记录总里程，再由行政经理结合起止地点导航里程进行校核。

因此第一期更适合采用以下模型：

- 记录里程：结束后里程减去出车前里程
- 导航核验里程：根据起止地点校核得到
- 核算里程：最终用于补助计算的里程

若后续出现多段停靠、复杂路线或跨城中转场景，再扩展“行程段明细”会更合适。

## 6. 异常流程建议

### 6.1 审批同步异常

- 业务单已创建但飞书审批实例创建失败时，申请单进入“审批发起异常”
- 系统应支持重试或人工补发审批

### 6.2 派车时资源已被占用

- 重新提示冲突原因
- 保持“审批通过待派车”状态
- 支持重新分配车辆或司机

### 6.3 消耗确认长期未处理

- 系统定时发送飞书提醒
- 必要时通知行政经理介入

## 7. 下一步设计输入

基于本文档，下一步可继续展开：

1. 数据实体关系设计
2. 核心字段清单
3. 页面原型
4. 接口清单
